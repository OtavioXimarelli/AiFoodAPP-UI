# Environment Variables Template
# Copy this file to .env.production or .env.development and configure accordingly

# =============================================================================
# API Configuration
# =============================================================================
# ⚠️ IMPORTANTE: Use a URL base SEM o sufixo /api
# O código adiciona /api automaticamente para endpoints REST
# OAuth2 precisa da URL sem /api (Spring Security intercepta antes do context-path)

# Production Environment (.env.production)
VITE_API_BASE_URL=https://api.aifoodapp.site
VITE_ENV=production
NODE_ENV=production

# Development/Staging Environment (.env.development)
# VITE_API_BASE_URL=https://api-dev.aifoodapp.site
# VITE_ENV=development
# NODE_ENV=development

# Local Development (vite dev server)
# VITE_API_BASE_URL=http://localhost:8080
# VITE_ENV=development
# NODE_ENV=development
